version: "3"

services:
  app:
    container_name: django
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
  background_tasks:
    container_name: process_task
    restart: always
    build:
      context: .
    volumes:
      - ./app:/app
    depends_on:
      - app
    command: >
      sh -c "python manage.py process_tasks --sleep=1 --log-std"